<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.text.*?>

<VBox alignment="CENTER" spacing="20.0" xmlns:fx="http://javafx.com/fxml"
      fx:controller="com.example.skrtdesk.controller.LoginController">
    <padding>
        <Insets top="40.0" right="40.0" bottom="40.0" left="40.0"/>
    </padding>
    
    <VBox alignment="CENTER" spacing="10.0" maxWidth="400.0">
        <Text text="SKRT Social" styleClass="title" />
        <Text text="Welcome back!" styleClass="subtitle" />
        
        <VBox spacing="15.0" VBox.vgrow="ALWAYS">
            <padding>
                <Insets top="30.0" bottom="10.0"/>
            </padding>
            
            <VBox spacing="5.0">
                <Label text="Email" />
                <TextField fx:id="emailField" promptText="Enter your email" />
            </VBox>
            
            <VBox spacing="5.0">
                <Label text="Password" />
                <PasswordField fx:id="passwordField" promptText="Enter your password" />
            </VBox>
            
            <Label fx:id="errorLabel" styleClass="error-label" visible="false" managed="false" />
            
            <Button fx:id="loginButton" text="Login" onAction="#handleLogin" 
                    styleClass="primary-button" maxWidth="Infinity" />
            
            <HBox alignment="CENTER" spacing="5.0">
                <Label text="Don't have an account?" />
                <Hyperlink text="Register here" onAction="#handleRegisterLink" />
            </HBox>
        </VBox>
    </VBox>
</VBox>

